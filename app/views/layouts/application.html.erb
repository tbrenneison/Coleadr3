<!DOCTYPE html>
<html>
<head>
 <title>Coleadr v.0.3</title>

<%= stylesheet_link_tag 'application' %>
<%= javascript_include_tag 'application' %>

</head>
<!--need 70px padding on the body because of the fixed-top navbar -->
<body style="padding-top: 70px">
	<div class="container-fluid">
		<!--need 70px body padding-top because of this, thanx Bootstrap-->
		<nav class="navbar navbar-default navbar-fixed-top">
		  <div class="container-fluid">
		    <!-- Brand and toggle get grouped for better mobile display -->
		    <div class="navbar-header">
		      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
		        <span class="sr-only">Toggle navigation</span>
		        <span class="icon-bar"></span>
		        <span class="icon-bar"></span>
		        <span class="icon-bar"></span>
		      </button>
				<h1 class="appName">CoLeadr</h1>
		    </div>
		    <!-- Collect the nav links, forms, and other content for toggling -->
		    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
		      <ul class="nav navbar-nav">
		        <li><a href="#">Link</a></li>
		        <li><a href="#">Link</a></li>
		      </ul>
		      <ul class="nav navbar-nav navbar-right">
						<% if user_signed_in? %>
						  <li>
					  		<%= link_to('Log Out', destroy_user_session_path, :method => :delete) %>        
					  		</li>
						<% else %>
					  		<li>
					  	 	  <%= link_to("Log In", new_user_session_path)  %>
					  		</li>
							<li>
								<%= link_to "Sign Up", new_account_path %>
							 </li>
						<% end %>
					<p class="notice"><%= notice %></p>
					<p class="alert"><%= alert %></p>
		      </ul>
		    </div><!-- /.navbar-collapse -->
		  </div><!-- /.container-fluid -->
		</nav>
		<hr>
	<%= yield %>
</div>
</body>
</html>
